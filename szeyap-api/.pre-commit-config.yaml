repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.14.9
  hooks:
    # Run the linter.
    - id: ruff-check
      args: ["check", "--select", "I", "--fix"]
    # Run the formatter.
    - id: ruff-format